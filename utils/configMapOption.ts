import { MapOptions } from "../types/mapTypes";

export const configMapOptions:MapOptions = {
    mapId: "b181cac70f27f5e6",
    disableDefaultUI: true,
    clickableIcons: true,
    zoomControl: true
}